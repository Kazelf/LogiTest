{
  "name": "logitest-ai",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "demo-system",
    "packages/shared"
  ],
  "scripts": {
    "build:shared": "npm run build --workspace @logitest/shared",
    "build:web": "npm run build --workspace web",
    "test:demo": "npm run test --workspace demo-system"
  }
}
